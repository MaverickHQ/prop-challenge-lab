# Copy to `.env` (git-ignored) and fill in. NEVER commit `.env` or paste
# values into chat.

# --- Comms (Phase 9) ---
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
OBSIDIAN_DEBRIEF_DIR=

# --- Data vendor (Phase 1; only if the vendor uses an API key) ---
DATA_VENDOR_API_KEY=
