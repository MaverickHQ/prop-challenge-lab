# Verdict #3 — 2026-07-06 — **NO-GO (validated, final) — with a real finding**

**Protocol:** v3, `b6b029c3134cf877` (git blob `098c4f2c…`) · **Runtime:**
30 s (9 cells) · **Family 2:** the failed-breakout fade · validity gates
armed and silent; nulls 0.00/0.01 while trading.

## Decision

**NO-GO on both instruments, independently, at G1 on the OOS fold.** Per
PREREG §8: validated no-go, no fee. This closes Family 2 and, with it,
the research program (two families, both honestly killed).

## The finding that makes this NO-GO different from the last one

Family 1 (ORB) never worked anywhere — nothing to overfit. Family 2 is
the opposite and more valuable result: **the fade's edge is real and it
persists out-of-sample** — it is simply too small for this challenge's
geometry at disciplined sizing.

Per-trade expectancy (net of costs, conservative fills), by fold:

| Cell (best) | dev | OOS | lockbox |
|---|---|---|---|
| MES k=0.2 unfiltered | +0.079R | **+0.107R** | **+0.141R** |
| MNQ k=0.2 unfiltered | +0.188R | **+0.171R** | +0.106R |
| …all 9 cells, both instruments | 34 of 36 fold-cells positive — no cell flips negative OOS on MES; MNQ stays positive everywhere | | |

- **No overfit signature**: MES OOS/lockbox expectancy came in *above*
  dev. The Phase-B screening did not mine noise; it found a persistent
  structural effect (94% of breakouts fail; the reversal pays).
- **But the challenge needs ~2× more.** Best P(pass): 0.41 (MES OOS,
  k=0.2 unfiltered) and 0.45/0.37 (MNQ dev/lockbox) vs the G1 bar of
  0.55. At ~+0.10R × 0.9 trades/day ≈ +$16/day expected drift against
  the ~+$33/day the target/horizon arithmetic requires — the edge is
  half-sized. G2 passes trivially (null ≈ 0); G1 fails everywhere; the
  gates did exactly their job of separating "real" from "enough".

## Ideas checked and rejected (so future-us doesn't re-litigate)

- **Double the risk** → the ruin math and the feasibility map both say
  breach probability explodes; refused (sealed guard).
- **Trade both instruments at once** (~1.8 setups/day) → MES/MNQ daily
  outcomes are highly correlated (same index complex): that is ~2× size,
  not 2 independent bets — drift and variance scale together and the
  two-barrier win probability *degrades* toward the zero-drift floor.
- **Longer horizon** → 120d already priced itself out of the economics
  gate (FEASIBILITY.md).
- **More parameters** → the refused failure, permanently.

## Program accounting (final)

Three sealed verdicts in one day: v1 void (instrument defect, caught),
v2 real NO-GO (family never worked), v3 real NO-GO (family works, not
enough). Fees paid: **$0**. Total spend: **≈ $32** data. Deliverables:
a validated harness, a feasibility frontier, and one genuine,
quantified, out-of-sample-persistent market finding (~+0.1R/trade fading
failed opening-range breakouts on index micros) that is worth knowing
and not worth this challenge's fee structure.

**What would reopen the program (PREREG v4+ territory):** a hypothesis
with *ex-ante* per-trade expectancy ≥ ~+0.25R net at ≥ 0.5 trades/day —
nothing smaller can clear the frontier. Anything else is the write-up.
